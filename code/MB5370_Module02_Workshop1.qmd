---
title: "Module 2: R4MS - Workshop 1"
author: "Katie Dyck"
date: "2026-06-03"
format:
  html:
    df-scroll: true
    df-print: paged
    embed-resources: true
    code-fold: show
    code-copy: hover
    code-block-bg: true
    code-block-border-left: "grey25"
    code-overflow: scroll
    toc: true
    number-sections: true
    number-depth: 4
    toc-location: left
    toc-expand: 3
    toc-title: Contents
    syntax-highlighting: github
    theme: cosmo
    title-block-banner: true
    fig-height: 6
    fig-width: 10
    fig-cap-location: bottom
---

# Introduction

This document includes all material covered from the first "R for Marine Science" workshop:

- Importing different file types (`.csv`, `.txt`, `.xlsx`)
- Foundational grammar for slicing, filtering, sorting, and transforming data
- Aggregating and summarizing data
- Visualizing the `penguins` dataset from the `palmerpenguins` package
- How to save and export datasets and plots

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  include = TRUE,
  message = FALSE,
  warning = FALSE)
```

```{r Connect to GitHub, include=FALSE}
# in the console:
#   credentials::git_credential_ask()
#   usethis::git_sitrep()

# if error message comes up that default branch is origin when it should be "main", enter this in the console:
#   usethis::git_default_branch_rediscover()

# in the console:
#   usethis::use_git()
#   usethis::use_github()

# in RStudio terminal:
  # git remote add origin https://github.com/katiedyck/MB5370_Module02.git
  # git push -u origin main
  # git push
```

Data files (.csv, .txt, .xlsx) were copied from the class repository "R4MarineScience" and saved in the [`data`](https://github.com/katiedyck/MB5370_Module02/tree/main/data) folder in my repository for this module.
-   `reef_cover_log.csv` contains information on underwater transects conducted at Nelly Bay, Geoffrey Bay, and Florence Bay on Magnetic Island, Queensland
-   `acoustic_telemetry_stream.txt` contains acoustic tracking data for sea turtles tagged in May 2026
-   `fish_catch_data.xlsx` contains information on fish species caught on the Great Barrier Reef
-   `mangrove_survey_raw.csv` contains information on mangroves on Hinchinbrook Island, Queensland

# Housekeeping

Purge the global environment with the command `rm(list = ls())`.

```{r Clear session environment}
#| output: false

# Inventory every active object currently residing in session RAM
objects()

# Purge global environment
rm(list = ls())

# Confirm that global session memory is now vacant
objects()
```

# Packages

The `tidyverse`, `readxl`, and `palmerpenguins` package libraries are needed for this workshop.

```{r Load packages}
library(tidyverse)
library(readxl)
library(palmerpenguins)
```

# Importing different file types

* .csv files can be imported with `read_csv()`.
* .txt files can be imported with `read_tsv()`.
* .xlsx files can be imported with `read_excel()`.

```{r Import data, echo=TRUE}
#| code-fold: false

benthic_cover <- read_csv(here::here("data/workshop1/reef_cover_log.csv"))
```

```{r}
benthic_cover
```

```{r echo=TRUE}
#| code-fold: false

acoustic_stream <- read_tsv(here::here("data/workshop1/acoustic_telemetry_stream.txt"))
```

```{r}
acoustic_stream
```

```{r echo=TRUE}
#| code-fold: false

fisheries_annual <- read_excel(here::here("data/workshop1/fish_catch_data.xlsx"), sheet = "Commercial_2026")
```

```{r}
fisheries_annual
```

```{r echo=TRUE}
#| code-fold: false

mangrove_data <- read_csv(here::here("data/workshop1/mangrove_survey_raw.csv"))
```

```{r echo=FALSE}
mangrove_data
```

For the mangrove data, we need to include additional arguments so that the first 5 lines of the dataset are ignored. These lines are just comments that are not part of the data.

```{r mangrove data, echo=TRUE}
#| code-fold: false

# Use args in read_csv() to read mangrove_data properly
mangrove_data <- read_csv(here::here("data/workshop1/mangrove_survey_raw.csv"),
                          skip = 5,       # skip the first 5 lines of field notes
                          na = c(".", "NA", "9999", "ND", "blank"))      # convert known text alternatives to true NA
mangrove_data
```

All 4 data sets are tibbles.
You can turn tibbles, which are `tidyverse` data objects, into a data frame:

```{r Covert tibble to data frame, echo=TRUE}
#| code-fold: false

benthic_cover_df <- as.data.frame(benthic_cover)

benthic_cover
benthic_cover_df
```

Notice the difference between these two data sets. `benthic_cover` tells you its dimensions and variable types (character, double, etc.) below the column headers. 


## Palmer Penguins dataset

The `penguins` dataset from the `palmerpenguins` package library contains measurements of penguin species found near Palmer Station in Antarctica.

```{r Load penguin data, echo=TRUE}
#| code-fold: true

# Load package data into active memory
data("penguins")

# Examine dataset structure
glimpse(penguins)       # tidyverse version

# Generate an exploratory summary matrix
summary(penguins)
```

* Note that the `bill_length_mm`, `bill_depth_mm`, `flipper_length_mm`, `body_mass_g`, and `sex` variables have some NA values.

# Foundational grammar: Slicing, filtering, sorting, and transforming

## `select()`

This verb allows you to vertically slice datasets, isolating specific columns by variable names.

```{r select(), echo=TRUE}
#| code-fold: false

# Vertically slice specific variables by name
morphology_metrics <- select(penguins, species, bill_length_mm, bill_depth_mm, body_mass_g)
morphology_metrics

# Retain a continuous block of attributes using colon operator
spatial_block <- select(penguins, species:island)
spatial_block

# Discard logistics tracking attributes while keeping everything else using minus sign
clean_scientific_fields <- select(penguins, -year)
clean_scientific_fields
```

## `filter()`

This verb isolates records (rows) horizontally based on conditional parameters. R evaluates each row against a logical expression, keeping entries that are true and dropping those that are false or NA (i.e. those that do not meet the conditions of the expression).

```{r filter(), echo=TRUE}
#| code-fold: false

# Isolate observations belonging to a single categorical target group
adelie_cohort <- filter(penguins, species == "Adelie")
adelie_cohort

# Filter out individuals using continuous numerical boundary thresholds
# Keeps only large penguins whose mass exceeds 4500 g
heavy_penguins <- filter(penguins, body_mass_g > 4500)
heavy_penguins

# Combine multiple conditional parameters across separate attributes
# Keep records that match Gentoo penguins sampled explicitly on Biscoe Island
biscoe_gentoo <- filter(penguins, species == "Gentoo" & island == "Biscoe")
biscoe_gentoo

# Search for rows matching multiple targeting flags within an explicit set
sub_islands <- filter(penguins, island %in% c("Dream", "Torgersen"))
sub_islands
```


## `arrange()`

This verb alters how the rows of data are sorted within your data frame, without changing individual cell values

```{r arrange(), echo=TRUE}
#| code-fold: false

# Sort penguins by ascending body mass (Default setting: smallest mass first)
lightest_first <- arrange(penguins, body_mass_g)
lightest_first

# Sort penguins in descending sequence using the desc() layout wrapper
heaviest_first <- arrange(penguins, desc(body_mass_g))
heaviest_first

# Execute nested sorting criteria: group by species, then sort by descending bill length
stratified_morphology <- arrange(penguins, species, desc(bill_length_mm))
stratified_morphology
```

## Pipe operator (`|>`)

The pipe operator is a great way to chain multiple operations together. You read from left to right, or top to bottom. You take the data, *then* filter it, *then* mutate it to create a new column, *then* select the columns you need.

The native R pipe syntax is `|>`, but there is another version built into the tidyverse with the `magrittr` package, which is also commonly used (`%>%`).

Instead of writing:
```{r no pipe operator, echo=TRUE}
#| code-fold: false

penguins_subset <- mutate(penguins, bill_ratio = bill_length_mm / bill_depth_mm)
penguins_final <- filter(penguins_subset, species == "Adelie")
```

You can just write:

```{r with pipe operator, echo=TRUE}
#| code-fold: false

penguins_final <- penguins |> 
    mutate(bill_ratio = bill_length_mm / bill_depth_mm) |> 
    filter(species == "Adelie")

penguins_final
```

## `mutate()`

This verb is used to calculate new variables based on ones that already exist in your data.

```{r mutate(), echo=TRUE}
#| code-fold: false

penguin_ratios <- penguins |> 
    mutate(body_mass_kg = body_mass_g / 1000,
           bill_ratio = bill_length_mm / bill_depth_mm)

penguin_ratios
```


# Data aggregation & ecological summarization

```{r group_by(), summarise(), echo=TRUE}
#| code-fold: false

# Grouping our active memory penguins by species
grouped_penguins <- group_by(penguins, species)

# Note that table looks similar, but metadata notes 'Groups: species [3]'
grouped_penguins

# Collapsing groups into explicit summary metrics
species_mass_summary <- summarise(grouped_penguins, 
                                  mean_mass_g = mean(body_mass_g))
species_mass_summary
```

Adelie and Gentoo species data includes NA values - this is an R design feature called the **Missing Value Trap**. Any mathematical function of any kind (sum, mean, sd, etc.) will automatically return NA to prevent miscalculations. This can be overridden by explicitly declaring that missing cells should be dropped during calculations.

```{r missing trap value, echo=TRUE}
#| code-fold: false

biological_signal <- penguins |> 
    group_by(species, sex) |> 
    summarise(
        sample_size = n(),    # count total number of individuals per category
        mean_mass_g = mean(body_mass_g, na.rm = TRUE),
        sd_mass_g = sd(body_mass_g, na.rm = TRUE)       # standard deviation calculation
    )

biological_signal
```


# Visualizing the `penguins` data

## Visualizing body size and shape

```{r visualizing body size & shape, echo=TRUE}
#| code-fold: true

penguin_mass_by_island <- penguins |> 
    group_by(species, island) |> 
    summarise(mean_mass_g = mean(body_mass_g, na.rm = TRUE))

penguin_mass_by_island

# Visualize the data
ggplot(penguin_mass_by_island, aes(x = species, y = mean_mass_g)) +
    geom_boxplot() +
    facet_wrap(~ island) +
    labs(x = "Species", y = "Mean Body Mass (g)", title = "Mean body mass of Penguin Species found at Palmer Research Station, Antarctica") +
    theme(plot.title = element_text(face = "bold"))
```

You can also pipe directly to visualization without creating an intermediate summary table:

```{r piping directly to visualization, echo=TRUE}
mass_compare_plot <- penguins |> 
    filter(!is.na(body_mass_g)) |>    # remove any NA values from body_mass_g
    group_by(species, island) |> 
    summarize(
        mean_mass = mean(body_mass_g, na.rm = TRUE),
        n = n(),
        sd_mass = sd(body_mass_g, na.rm = TRUE),
        .groups = "drop"
    ) |> 
    ggplot(aes(x = species, y = mean_mass, colour = island)) +
    geom_point(size = 3, position = position_dodge(width = 0.5)) +
    geom_errorbar(aes(ymin = mean_mass - sd_mass,
                      ymax = mean_mass + sd_mass),
                  width = 0.2,
                  position = position_dodge(width = 0.5)) +
    labs(title = "Mean Body Mass by Species and Island",
         x = "Species",
         y = "Mean Body Mass (g) ± SD ") +
    theme_minimal() +
    theme(plot.title = element_text(face = "bold"))

mass_compare_plot
```

## **Completed morphological plot and summary table**

```{r echo=TRUE}
#| code-fold: true

mass_compare_plot2 <- penguins |> 
    filter(!is.na(body_mass_g)) |>    # remove any NA values from body_mass_g
    group_by(species, island) |> 
    summarize(
        mean_mass = mean(body_mass_g, na.rm = TRUE),
        n = n(),
        sd_mass = sd(body_mass_g, na.rm = TRUE),
        se_mass = sd_mass / sqrt(n),
        .groups = "drop"
    ) |> 
    ggplot(aes(x = species, y = mean_mass, colour = island)) +
    geom_point(size = 2, position = position_dodge(width = 0.5)) +
    geom_errorbar(aes(ymin = mean_mass - se_mass,
                      ymax = mean_mass + se_mass),
                  width = 0.2,
                  position = position_dodge(width = 0.5)) +
    labs(title = "Mean Body Mass by Species and Island",
         x = "Species",
         y = "Mean Body Mass (g) ± SE ") +
    theme_minimal() +
    theme(plot.title = element_text(face = "bold"))

mass_compare_plot2
```

When we compare the error bar graphs side-by-side, we see that the standard *error* bars are much smaller than the standard *deviation* bars, which makes sense. Standard error is a measure of how precisely we have estimated the mean, while standard deviation is a measure of how much variability there is in the data. The standard error will always be smaller than the standard deviation, especially as the sample size increases.

```{r, echo=TRUE}
#| code-fold: true

summary_SD <- penguins |> 
    filter(!is.na(body_mass_g)) |>    # remove any NA values from body_mass_g
    group_by(species, island) |> 
    summarize(
        mean_mass = mean(body_mass_g, na.rm = TRUE),
        n = n(),
        sd_mass = sd(body_mass_g, na.rm = TRUE),
        .groups = "drop"
    )

summary_SE <- penguins |> 
    filter(!is.na(body_mass_g)) |>    # remove any NA values from body_mass_g
    group_by(species, island) |> 
    summarize(
        mean_mass = mean(body_mass_g, na.rm = TRUE),
        n = n(),
        sd_mass = sd(body_mass_g, na.rm = TRUE),
        se_mass = sd_mass / sqrt(n),
        .groups = "drop"
    )

summary_SE
```

Looking at the summary table, we can see that the standard error decreases as sample size (n) increases (e.g. SE 73.5 for Adelie penguins on Biscoe island (n = 44) vs. SE 45.5 for Gentoo penguins on Biscoe island (n = 68)). This table also tell us that as n increases, so does the mean mass for each species on each island, which makes sense.


# Saving, exporting, and version milestones

We can save our figures and tables to our GitHub repository by creating an "output" folder in our directory.

```{r Saving tables, echo=TRUE}
#| code-fold: true

# Create output directories if they do not exist:
if (!dir.exists("/outputs/figures")) dir.create("outputs/figures", recursive = TRUE)   # folder for figs
if (!dir.exists("/outputs/tables")) dir.create("outputs/tables", recursive = TRUE)     # folder for tables
if (!dir.exists("/Rdata")) dir.create("Rdata", recursive = TRUE)   # folder for Rdata objects

# 1. Exporting our collapsed summary table as a universal flat text file
write_csv(biological_signal, "outputs/tables/penguin_species_mass_summary.csv")

# 2. Saving our cleaned morphological cohort table as a native R binary file
saveRDS(clean_scientific_fields, "outputs/clean_penguin_morphology_cohort.rds")
```

```{r Saving figures}
ggsave("outputs/figures/mass_compare_plot.png", 
       plot = mass_compare_plot, 
       width = 120, height = 120, 
       units = "mm", dpi = 300)

ggsave("outputs/figures/mass_compare_plot2.png", 
       plot = mass_compare_plot, 
       width = 120, height = 120, 
       units = "mm", dpi = 300)
```
